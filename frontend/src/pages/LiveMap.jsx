import { Link } from 'react-router-dom'
import { ArrowLeft, MapPinned, ShieldAlert, TrafficCone, TrendingUp } from 'lucide-react'
import CityMap from '../components/map/CityMap'
import { SectionHeading } from '../components/ui'
export default function LiveMap({ incidents, roadIssues, hotspots, observations }) { return <div className="page"><div className="page-top"><div><div className="eyebrow">Geospatial intelligence</div><h1>Live city map</h1><p>Every signal, located and ready for action.</p></div><Link className="ghost-btn" to="/"><ArrowLeft size={16} /> Back to overview</Link></div><section className="panel full-map-panel"><SectionHeading eyebrow="Delhi operations view" title="Urban sensing coverage" description="Use the layers to isolate incidents, infrastructure or traffic patterns." /><div className="full-map"><CityMap incidents={incidents} roadIssues={roadIssues} hotspots={hotspots} observations={observations} /></div><div className="map-stats"><MapStat label="Incidents" value={incidents.length} icon={ShieldAlert} tone="red" /><MapStat label="Road issues" value={roadIssues.length} icon={TrafficCone} tone="amber" /><MapStat label="Traffic zones" value={hotspots.length} icon={TrendingUp} tone="blue" /></div></section></div> }
function MapStat({ label, value, icon: Icon, tone }) { return <div className={`map-stat tone-${tone}`}><span><Icon size={17} /></span><div><strong>{value || '—'}</strong><small>{label}</small></div></div> }
